{El objetivo del test es probar el error de poner dos sentencias 
en una etiqueta sin estar encerradas por begin end

Resultado esperado: Test Fallido
Error Sintactico en linea 15 : Se espera un :
}
program prog;

	const A = 34;
	  B = 55;
	
begin
 case t of 
 g: y:=h;
	d:=u;
 
 end;
end.
