{El objetivo del test es probar el error de omitir el end
de una sentencia compuesta dentro de un while

Resultado esperado: Test Fallido
Error Sintactico en linea 18 : Se espera un end
}
program prog;

	const A = 34;
	  B = 55;
begin
   while (e <succ(u)) do
   begin
   g:= go;
   g:=8
  
   
end.
