{
El proposito es probar una def de variable,
seguida de una def de funcion con parametros
y un bloque con una asignacion.

Resultado esperado: Test Valido
}
program p;
type t=integer;
var n: boolean;
 j:integer;
function p2 (var x,s,d:T):boolean;
 begin
 end; 
begin
 j:=7;
 write(j,j,j);
 writeln;
end.
