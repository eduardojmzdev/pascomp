{El objetivo del test es ejecutar un programa con todos 
los simbolos especiales que forman parte del lenguaje.
Resultado esperado: Test Valido
Tokens: todos los tokens (ver informe)
}
program prog;

	const A = 34;
	  B = 55;
	function p:bloo;
	begin   end;	
	var
		x,y,z:+boolean..16; 
		y,l:array[+6..-45] of true; 
		x:booleann;
	function f1(var z,d,e:integer; m,n : boolean):boolean;
	begin 
		case (4+5) div 3 of 	
			a:A[5]:= 8;
			l:
		end
		
	end;	
	procedure p2(var z:integer);
	begin 
		
	end;	
	
begin
  if (x<y) or (x<=y) and (x>=y) or (x>y) and (not (x=y)) then
     while(x<=y) do
	     t3[x]:=y
  else 
	begin
     case (x+y-x * x)div y of 
	    1: ;
		2:begin x:=234;end
	end
  end
  
end.