program SentI;
const b=true;
      i=123;
var a :integer;
begin
 while (pred(a)=pred(i)) or b do 
   ;
 while succ(maxint) >pred(a) do
  ;
 while pred(123 div 3) do{se espera una expresion booleana}
 ;
end.