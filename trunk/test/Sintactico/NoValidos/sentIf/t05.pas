{El objetivo del test es probar el error de omitir la 
expresion condicional de un if 

Resultado esperado: Test Fallido
Error Sintactico en linea 34 : Se espera una expresion
}
program prog;

	const A = 34;
	  B = 55;
	
begin
 if   then g:= 5;
  
end.
