{El objetivo del test es probar el error de omitir el begin 
de una sentencia compuesta dentro de un while

Resultado esperado: Test Fallido
Error Sintactico en linea 14 : Se espera un '.'
}
program prog;

	const A = 34;
	  B = 55;
begin
   while (e <succ(u)) do
   g:= go;
   g:=8
   end
   
end.
